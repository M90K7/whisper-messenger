{
  "name": "whisper-messenger",
  "scripts": {
    "mig": "dotnet ef migrations add InitialCreate",
    "mig:update": "dotnet ef database update",
    "publish": "dotnet publish -o ./bin/publish"
  }
}